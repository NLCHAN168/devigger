/*let array = [ 
	{
		"The pl": [
		 { bet365: "+300", ..., pinged: false},
		 { ... }
		]
		"last updated": ...
	}
]

let { event_name, last_updated, odds } = res;
let fixedOdds = odds.map(original => original["pinged"] = false);
let obj = {
	event_name: event_name,
	odds: fixedOdds,
	last_updated: last_updated
};

pgawin.push(obj);

let whatever = functionCall();

function compareOldAndNew(old, new) {
	for (let i = 0; i < old.odds.length; i++) {
		if (old.odds[i].draftkings !== new.odds[i].draftkings) {
			if (old.odds[i].draftkings > new.odds[i].draftkings) return "hello";
			console.log("Hello!")
	}
	console.log("End");
}
}*/
